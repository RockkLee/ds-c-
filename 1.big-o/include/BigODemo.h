// Add these macros for include guard, 
// which is used to prevent multiple inclusions of the header files
#ifndef BIGODEMO_H
#define BIGODEMO_H

//create a class
class BigODemo
{
    public:
        BigODemo();
        void run();
        void bigoNSquare();
        void bigoLogN();

    private:
        void binarySearch();
        void bianrySearchTree();
};

// Add these macros for include guard, 
// which is used to prevent multiple inclusions of the header files
#endif // BIGODEMO_H